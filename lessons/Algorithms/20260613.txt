课后作业:
    1. 通过Debug理解Prim算法, 标注算法本身
        1.1. 如果图用dict[str: dict[str: int]]存储, 再次实现Prim算法
    2. 通过带权值的邻接矩阵, 实现有向图的以下功能:
        2.1. 有向图用{节点1 : {
                                出边 : {出节点1 : 权值1 , 出节点2 : 权值2 ...},
                                入边 : {入节点1 : 权值1 , 入节点2 : 权值2 ...}
                            },
                     节点2 : {
                                出边 : {...},
                                入边 : {...}
                            },
                     ...}存储
        2.2. 实现Nodes(G:dict)
        2.3. 实现InEdges(G:dict,node)
        2.4. 实现OutEdges(G:dict,node)
        2.5. 实现有向边<u,v>的权重选取GetWeight(G:dict,u,v)
